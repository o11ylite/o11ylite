apiVersion: v1
kind: Service
metadata:
  name: {{ include "o11ylite.fullname" . }}
  labels:
    {{- include "o11ylite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "o11ylite.selectorLabels" . | nindent 4 }}
